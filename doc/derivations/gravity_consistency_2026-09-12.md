# Action-consistent gravity normalization and identifiability

2026-09-12. Module5 equations and controls frozen before implementation. This repairs the stated gravitational action and its direct conversions; it does not derive dynamical spacetime, inflation, Newton's constant or a cosmological constant from the Bose ring. The underlying induced coefficient is conditional on supplied field content, regulator and subtraction assumptions.

## Jordan action to Einstein potential

In natural units take L=sqrt(-g)[M² R/2+alpha R²/2], M>0, alpha>0, with reduced M²=1/(8pi G). Introduce an auxiliary chi: alpha R²/2 -> alpha chi R-alpha chi²/2. Its equation gives chi=R. Let F=1+2alpha chi/M²>0 and use g_E=F g_J. The canonical scalar is phi=sqrt(3/2) M log F. The Einstein potential is

V(phi)=M⁴/(8alpha)[1-exp(-sqrt(2/3)phi/M)]².

Expansion at phi0 gives V= M² phi²/(12alpha)+O(phi³), hence m²=M²/(6alpha). Plateau V0=M⁴/(8alpha). The leading large-e-fold approximation epsilon=3/(4 Ne²), together with As=V/(24pi² M⁴ epsilon), gives As=Ne²/(144pi² alpha). The inverse alpha=Ne²/(144pi² As) is calibration, not prediction. The old mass 1/(2sqrt(alpha)), plateau3/(16alpha), and denominator96pi² were inconsistent with this action. n_s=1-2/Ne and r=12/Ne² remain leading approximations. No candidate enhancement factor or observational target is adjusted to restore an old numerical near-match. These leading large-Ne formulas are not exact finite-Ne evolution. Within potential slow roll, x=exp(sqrt(2/3)phi/M), epsilon=4/[3(x-1)²], and Ne=(3/4)[x-x_end-log(x/x_end)] with x_end=1+2/sqrt(3). Finite-Ne potential slow roll itself differs from exact perturbation evolution. The frozen small-alpha controls may give As>=1 and are algebraic tests, not a justified weak-perturbation cosmological regime.

## Reduced Planck scale and Wald entropy

The inherited induced coefficient M²=p Lambda²/(48pi²) is reduced. If M_E and Lambda are energies in joules, G=hbar c^5/(8pi M_E²). The unreduced Planck energy is sqrt(8pi) M_E. With a=hbar c/Lambda and physical lP²=hbar G/c³, a/lP=sqrt(p/(6pi)). This differs by sqrt(8pi) from the old spacing conversion; the induced reduced M/Lambda ratio itself remains sqrt(p/(48pi²)).

Einstein–Hilbert Wald entropy in natural units is S=2pi A M², with A the Einstein-frame horizon area. This is not the full Jordan-frame R+R² expression at nonzero curvature: S_J=2pi integral_horizon(M²+2alpha R_J)dA_J. Since dA_E=F dA_J, that expression agrees with the Einstein-frame area formula. Restoring energy-length units gives S=2pi A M_E²/(hbar c)²=A/(4lP²). Preserve the existing numerical entropy function and fixed physical lP anchor. Inverse cutoff matching is not a predicted absolute scale. Repair duplicate spacing and the two direct Newton-conversion callers so their units agree, retaining explicit-cutoff APIs.

## Identifiability calculations

A constant propagator amplitude A(k)=Z G/k² depends only on ZG. For any a>0, Z->a Z and G->G/a preserve it. In logarithmic coordinates the gradient of log(ZG) is (1,1), with null vector (1,-1). Thus a constant normalization is degenerate with a calibrated gravitational coefficient, not separately measurable from this observable.

Write the effective reduced coefficient M_eff²=b+c+p Lambda²/(48pi²), where b and c are independent bare and counterterm contributions (real), and Lambda>0 is a supplied cutoff. Let k=p/(48pi²). Exact transformations (b,c)->(b+t,c-t), and (Lambda,b)->(Lambda',b+k(Lambda²-Lambda'²)) leave M_eff² invariant. The Jacobian with coordinates (b,c,Lambda) is (1,1,2k Lambda). Null directions (1,-1,0) and (-2k Lambda,0,1) exhibit local nonidentifiability. The latter means delta b=-2k Lambda delta Lambda, delta c=0; coordinate entries have their corresponding units, not interchangeable dimensions. With b,c retained, a/lP=sqrt(8pi M_eff²/Lambda²), not a function of p alone. Physical report requires M_eff²>0; it cannot infer b=c=0. These are dimensionally declared natural-unit coefficients, not fitted empirical values.

## Flat-space energy origin versus gravity

For any finite Hermitian H and real scalar E, H'=H+E I has the same gaps and conjugation evolution exp(-itH') A exp(itH')=exp(-itH) A exp(itH). State vectors acquire only a global phase. A global identity shift on the direct sum of number sectors also preserves population ordering; a number-dependent shift is not global and may change it. Once an independent gravitational action couples a vacuum energy density rho_v through -sqrt(-g) rho_v, its stress tensor is -rho_v g_mu_nu and it shifts Lambda_cosmo by rho_v/M_eff². A finite-ring energy offset does not specify a covariant energy density, physical volume, subtraction prescription or gravitational coupling. No cosmological-constant prediction follows.

## Frozen controls and acceptance

Dimensionless M=1,2; alpha=.5,2,10; phi/M=0,.1,1,5; Ne=40,55,70; arbitrary inverse amplitudes1e-3,1e-6 (synthetic calibration controls, not experimental targets). Reduced/unreduced round trips at energies1,2,10 and p=5,11; verify direct legacy helpers with their existing default p without modifying its value. Z,G positive controls (2,3), transformation factors .25,4. Induced b=2,c=-.5,p=5,Lambda=3 and transformed cutoff2/4; b/c shifts+-.25. Flat H=[[1,.2],[.2,2]], global offsets-3,4, times0,.3,1, noncommuting Hermitian source. Dense flat-space oracle cap32; no production simulation. Test positive/finite domains, representability and strict JSON; explicitly separate exact formulas, leading slow roll, numerical residuals and calibration.

New module/test/demo gravity_consistency; narrow repairs to graviton_propagator, emergent_spacetime, direct pipeline/bridge conversions and affected normalization tests/docs only. Derive expected values independently rather than copying wrong legacy tests. Retain fixed enhancement candidates and let statuses change. Supersede directly affected stale documentation claims rather than preserve old near-matches. Independent mathematical and implementation review required. Stdout-only text/strict-JSON demo from an empty directory.

## Independent mathematical review

Independent symbolic review confirmed the auxiliary-field elimination, potential expansion and leading amplitude; independent dimensional checks confirmed SI G and spacing. It required explicit EH/Einstein-frame Wald scope and real identity offsets, now stated above. The full Jordan entropy includes its R² derivative contribution. The fixed physical lP and separately stored G have independent rounding (relative difference approximately2.96e-8); tests compare the exact conversion of the same anchor, not falsely demand roundoff agreement between rounded constants. The supplied integer p is a sector/mode multiplicity, not a fitted continuous coefficient; scalar paths need no arbitrary allocation cap on p but reject an integer if its float conversion loses it.

## Executed verification record

The first independent new-module run passed93 tests with three failures in tests that compared two separately rounded legacy Planck-length anchors as identical. No physical constant was changed. The tests now independently enforce each module's exact anchor conversion and the ratio between their retained anchors. All96 new-module tests and71 directly affected legacy graviton tests then passed together (167 total) with warnings as errors. The symbolic test independently derives auxiliary elimination, V''(0) and the144pi² denominator; direct pipeline checks stub unrelated substrate evolution and test only its calibrated G conversion. Four additional vacuum-shift/population-order checks passed in the expanded171-test focused/legacy run. The wider bounded regression passed1,436 tests before those four additions, followed by all eight algebra-check groups. The gravity demo passed text/strictJSON from an empty temporary directory with no files/stderr; new files passed Python3.8 grammar/compilation. Independent legacy integration review confirmed action factors, direct conversions and unchanged candidate dictionaries/thresholds. It found an extreme-input regression: evaluating sqrt(6alpha) overflowed for finite alpha=1e308 and returned a false zero mass. Both legacy mass properties now evaluate sqrt(6)*sqrt(alpha); added1e308/max-float regressions compare an independent logarithmic oracle. The legacy focused suite then passed73 tests. Review also found one stale CS/WZW status sentence in the scalaron derivation; it is corrected to the actual open status. The mass fix passed independent73-test review and a70-digit Decimal oracle at extreme alpha. The parent combined run passed173 tests after the two added mass checks.

Independent new-diagnostic review identified three unresolved-arithmetic defects: large bare/counterterm or cutoff transformations could destroy the original coefficient; cancellation in b+c+induced could return an inaccurate positive coefficient; and an identity-dominated original Hamiltonian could have a collapsed numerical spectrum while a shifted matrix resolved its true gap. The corresponding regression controls are retained rather than changing the physical examples. The effective coefficient now uses compensated summation and rejects when the heuristic 16eps sum|terms| exceeds 1e-10 times the physical coefficient. Finite transformations condition both coefficients and compare invariance within256eps relative to the physical coefficient, never the large cancelling terms. Both original and shifted Hamiltonians must resolve their nonscalar parts/nonzero gaps above64eps times the full norm; independent centered eigensystems check gaps within512eps times the centered norm. These are conservative numerical rejection policies, not certified roundoff bounds. The parent post-fix focused run passed177 tests (104 new and73 legacy). Independent re-review passed104 tests and verified all three reproductions, adjacent controls and a200-case transformation probe; no additional confirmed defect remained. The final gravity text/strictJSON demo again passed from an empty directory without files/stderr, and all three new Python files passed Python3.8 syntax/compilation.

The final post-fix bounded regression passed1,424 tests with warnings as errors, followed by all eight algebra-check groups. This explicitly selected all tracked test_substrate_* files plus gauge_heat_kernel, quantum_flavor_sources, graviton_propagator and the new gravity_consistency tests; it is a different selection from the earlier1,436-test run, not a full-repository claim.

## Publication dependency

Module4 is committed/pushed f53503f in open PR27, stacked on PR26/25/24. Ordinary PR24 merge remains denied; none is claimed merged. This module uses dependent branch science/gravity-consistency. No bypass, instruction/configuration edits, benchmark mining or physical target fitting.
