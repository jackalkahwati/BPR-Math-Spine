# Fixed-content anomaly constraints and reduction information loss

2026-09-12. Foundational campaign module4, following verified module3 and open dependent PR38. Independent charged/parent derivations and adversarial contract review are complete. Equations, controls and API were frozen before implementation and independently authored tests began. Implementation's single bounded author smoke and the first independent focused suite passed (167 tests). Separate static and executed exact-code review found no blocker. The combined27-suite regression passed3020 tests with unchanged source/demo/test hashes. Grammar, isolated demos and eight algebra groups passed; scientific verification is complete. Publication is recorded separately in the campaign ledger.

## Scope

Use only the already supplied left-handed Q,u^c,d^c,L,e^c representations and optional existing neutral singlet. No search over new hypercharges, cancellation fields, families or physical chirality mechanisms. Anomaly cancellation is a consistency condition within these stipulated representations; it does not derive them from the Bose ring, establish physical spacetime fermions or select three generations.

Separately use the supplied chiral parent and its restricted sphere pushforward. The fixed parent invariant domain is ordered(S4,S2²,S2X²,X^4,p1S2,p1X²,p1²,p2). Determine the linear reduction's image and kernel, not merely whether one supplied polynomial vanishes after reduction. Terms invisible to the restricted reduction can still obstruct parent consistency.

## Exact targets

Reconstruct rational anomaly coefficients independently from representation dimensions, cubic/index signs and the supplied hypercharges. Include the relevant local gauge and mixed gravitational anomaly rows with their conventions explicitly stated. Derive the rank, rational kernel, nonnegative-integer multiplicity solutions and global SU2 Witten parity constraint. Rank4 and equal charged multiplicities are targets to verify, not premises. The neutral singlet is expected to contribute a zero column; its unconstrained multiplicity must be distinguished from the charged kernel.

For the eight fixed parent invariants derive the exact pushforward matrix in a specified independent reduced invariant basis. Compare zero flux with nonzero flux, using explicit0,+1,-1 controls. Display basis directions and combinations lost by the map, including p2 if confirmed. Reduced cancellation cannot certify the lone anomalous parent; no completion mechanism is added.

## Independently derived charged matrix

Fix column order(Q,uc,dc,L,ec) with all species left-handed and the supplied hypercharges(1/6,-2/3,1/3,-1/2,1). In row order(SU3_cubic,SU3_squared_Y,SU2_squared_Y,Y_cubic,gravity_squared_Y), the rational matrix is

```
( 2,    -1,    -1,     0, 0)
(1/6, -1/3,   1/6,     0, 0)
(1/4,    0,     0,  -1/4, 0)
(1/36,-8/9,   1/9,  -1/4, 1)
( 1,    -2,     1,    -1, 1)
```

The quadratic fundamental index is1/2, while the SU3 fundamental cubic coefficient is+1 and antifundamental-1. Other-group dimensions multiply each coefficient. The cubic hypercharge row equals(2/9)row1-4row2-3row3+row5. Rows1,2 force nuc=ndc=nQ; row3 gives nL=nQ; row5 gives nec=nQ. Thus rational kernel is span(1,1,1,1,1), primitive integer solutions are integer multiples of that vector, and physical nonnegative solutions require the multiplier to be a nonnegative integer. A four-row minor using rows1,2,3,5 and columnsuc,dc,L,ec has determinant1/8, an independent rank4 witness. No positive family count is selected.

The omitted local structures vanish for stated representation-theoretic reasons: SU2 has no cubic invariant, non-Abelian generators are traceless so SU3 Y²/SU2 Y² and cross-simple-factor traces vanish, and there is no pure perturbative gravitational anomaly in4D. Since the unique dependence involves all five displayed rows, dropping any one can leave rank4; rank/kernel agreement alone therefore cannot verify every row. Tests must reconstruct and compare each coefficient and include row-isolating controls.

The optional neutral singlet adds an exactly zero column, with its own independent multiplicity. Witten parity is(3nQ+nL) modulo2, automatically even on the charged kernel because it is4 times the common multiplicity. This global constraint is evaluated only on integer multiplicities; it is not appended as a rational linear row or inferred from fractional multiplicities.

## Reviewed restricted reduction conventions

Use X_b to denote the base Abelian two-form. The inherited source symbol named X4 denotes this base two-form; it must not be confused with the domain monomial X^4. Use explicit domain labels S4,S2_squared,S2_X_squared,X_fourth,p1_S2,p1_X_squared,p1_squared,p2. The proposed reduced order is(S2_Xb,Xb_cubed,p1_Xb). With X=X_b+m eta, eta²=0 and integral_sphere eta=1, only domain columns3,4,6 contribute: (c3,c4,c6) map to(2m c3,4m c4,2m c6). The domain describes formal eight-forms; the target is the formal six-form local anomaly polynomial, not a literal nonzero six-form on a physical four-dimensional base.

For nonzero m the rational image is all three target directions and the kernel is span(e1,e2,e5,e7,e8), including p2. At m=0 the entire domain lies in the kernel. Over the declared integer coefficient lattice the nonzero image is2m Z direct_sum4m Z direct_sum2m Z, not all of Z³. This lattice statement is a formal basis-dependent arithmetic property, not a derived physical anomaly-quantization law. Essential assumptions are product tangent geometry, Spin10 invariants and base tangent characteristic classes pulled back from the base; the Abelian X instead explicitly has vertical flux. Changing these assumptions changes the reduction problem. Both independent reductions agree on this matrix, its rational kernel/image and its distinct formal integer-image lattice. The parent coefficients and lost residual are recorded below.

## Reviewed parent coefficients and lost information

Independent weight sums give Tr F²=4S2 and Tr F^4=3S2²-2S4 for the supplied16 spinor weights. The parent coefficient vector is s*(-1/12,1/8,Q²,2Q^4/3,-1/12,-Q²/3,7/360,-1/90). Its image is s*m*(2Q²,8Q^4/3,-2Q²/3). This module's parent control fixes the inherited charge Q=1 and chirality s=+1; neither is fitted or scanned. Projection onto the invisible coordinates leaves(-1/12,1/8,0,0,-1/12,0,7/360,-1/90), with nonzero p2 coefficient-1/90. Both independent parent derivations agree.

The vanishing internal4-form p1(TS2) implies that p2(Tbase direct_sum TS2) supplies no internal degree2 term. This is why p2 is invisible under this restricted product-background pushforward, not why the formal p2 anomaly vanishes. Formal descent-polynomial degrees are retained even when larger than the physical base dimension. At nonzero flux the fixed charge1 parent's reduced polynomial itself is nonzero; at flux0 the entire reduction is zero while the parent remains nonzero. No additional U1/global/higher-dimensional consistency follows from the charged SM rows or Witten parity.

## Frozen controls and API

Independent charged-content and parent derivations, adversarial review and final API review accept this contract before implementation or control evaluation. Clarifications distinguish X^4 from inherited X4, non-Abelian pulled-back invariants from flux-carrying X, row subsets from field subsets, and formal integer lattice fields from rational coordinates. No additional representation, charge or flux scan is adopted.

Use the existing representation labels Q,uc,dc,L,ec and optional nc; row labels SU3_cubic,SU3_squared_Y,SU2_squared_Y,Y_cubic,gravity_squared_Y. No generic representation or hypercharge input. Four-row subset controls must not silently replace the full five-row equations. Parent domain labels are those above; codomain labels(S2_Xb,Xb_cubed,p1_Xb).

All arithmetic uses Python Fraction and exact integers. Public system matrices/bases are immutable tuples containing Fraction values, not NumPy arrays. Reports are detached JSON-native dictionaries with every rational number serialized canonically as str(Fraction), including '0' and '1' for integers; ranks, dimensions, multiplicities, flux, index lists, integer_image_steps and integer_image_index remain JSON integers (or None where specified). Demo encodes primitive exact system dictionaries recursively; JSON never uses rational-to-float conversion. No NumericalUnavailable class is needed: all supported operations are finite exact arithmetic. Invalid domains raise ValueError. No eigensolver, SVD, floating rank or tolerance.

Scalar integer domains accept builtin int and NumPy integer values, excluding bool/np.bool_; no float/string/complex/custom-coercion inputs. `include_singlet` must be builtin bool. Fixed-length inputs must be builtin list or tuple checked for length before inspecting elements. Multiplicities are integers0..1,000,000. Coefficients for pushforward are builtin integers or Fraction (NumPy integers accepted after safe type validation); absolute canonical numerator<=1,000,000 and denominator<=1,000,000. Reject booleans, floating approximations, strings, iterators and custom numeric objects. Flux is integer in[-16,16], a computational bound not a flux quantization prediction. Named limits MAX_MULTIPLICITY=1_000_000,MAX_COEFFICIENT_COMPONENT=1_000_000,MAX_ABS_FLUX=16. Validate complete inputs before zero-flux dispatch.

- `anomaly_system(include_singlet=False)`: dictionary columns,row_labels,matrix,rank,nullity,rref,pivot_columns,kernel_basis,primitive_charged_generator,rank_minor,rank_minor_determinant,row_dependence_coefficients,exact_arithmetic=True. Matrix/rref tuple rows of Fractions; kernel_basis tuple of column vectors. Pivot columns and minor row/column indices zero-based. rank_minor={rows:(0,1,2,4),columns:(1,2,3,4),matrix:tuple_rows}. Dependence coefficients(2/9,-4,-3,1) multiply rows0,1,2,4 to yield row3. Kernel bases are positive primitive charged vector and, if nc enabled, nc unit vector, in that order (not blindly signed generic RREF output). Charged generator in the nc system has last component0. Rank/nullity/RREF computed by bounded exact elimination, not floating or assumed labels.
- `multiplicity_report(multiplicities,include_singlet=False)`: lengths5 or6 matching flag. Returns columns,multiplicities,anomalies (row-label to rational-string),local_anomaly_free,witten_doublet_count,witten_parity,witten_anomaly_free,fully_consistent_with_tested_constraints,charged_multiplicities_equal,family_count (common charged integer only if all equal, otherwise None),singlet_count (None when not included),scope,exact_arithmetic=True. 'Fully' is explicitly scoped to these five local rows plus this SU2 parity, not parent/all-global consistency. No inference that family_count3 is selected.
- `reduction_system(flux)`: returns flux,domain_basis,codomain_basis,matrix,rank,nullity,rref,pivot_columns,kernel_basis,image_basis,integer_image_steps,integer_image_index,scope,exact_arithmetic=True. Matrix3x8; for nonzero flux kernel coordinate units0,1,4,6,7 in that order, image_basis standard rational target units (not scaled columns); at zero kernel all8 and image empty. integer_image_steps=(abs(2m),abs(4m),abs(2m)) for nonzero; zero steps(0,0,0), integer_image_index=None for infinite index at zero, otherwise16 abs(m)^3. These integer-lattice fields are basis-dependent formal arithmetic, not physical quantization. Compute rank/RREF/kernel through exact bounded elimination; image units follow its pivot columns with explicit normalization.
- `pushforward(coefficients,flux)`: exact length8 vector mapped to immutable tuple3 Fractions by the reviewed map. Input validation precedes even zero-flux return. No SymPy expressions or arbitrary monomials accepted.
- `parent_report(flux)`: fixed Q=1,s=+1; returns flux,charge=1,chirality=1,domain_basis,codomain_basis,parent_coefficients,reduced_coefficients,visible_coefficients,invisible_coefficients,invisible_pushforward,parent_nonzero,reduced_nonzero,p2_coefficient,split_convention='nonzero_flux_coordinate_split',restricted_reduction_certifies_parent=False,exact_arithmetic=True. Vectors rational-string lists. visible_coefficients retain columns2,3,5 of parent and invisible retain0,1,4,6,7 for every flux, including zero. At flux0 these are fixed coordinate splits of the nonzero-flux map, NOT a claimed direct-sum complement to its larger zero-flux kernel; document split_convention='nonzero_flux_coordinate_split'.
- `demonstration_report()`: keys anomaly_systems,multiplicity_cases,reduction_cases,parent_cases,limitations,arithmetic_domain. anomaly_systems is a dictionary with encoded systems under literal keys without_singlet and with_singlet. multiplicity_cases and parent_cases are lists of bare report dictionaries; reduction_cases is a list of dictionaries with keys system,basis_images,kernel_images; limitations is a list of strings. arithmetic_domain={kind:'exact_rational',max_multiplicity:1000000,max_coefficient_component:1000000,max_abs_flux:16,rational_encoding:'canonical_fraction_string'}. Import path bpr.chiral_content_constraints; demo scripts/demo_chiral_content_constraints.py. These container/key details were clarified while authoring, before the first implementation smoke or test run. Fixed multiplicity cases without nc: zero vector; t*(1,1,1,1,1) for t1,2,3; five singleton unit vectors; and cubic-blind control(32,1,0,0,0), whose Y³ row vanishes but other local rows do not. With nc: (1,1,1,1,1,0),(1,1,1,1,1,2),(0,0,0,0,0,1). Total13 cases. reduction_cases and parent_cases each flux0,+1,-1 in that order. Each reduction demo case includes system, eight basis_images from pushforward(unit,flux), and kernel_images; all exact strings. No parameter or representation sweep. Boundary inputs ±16, maximum multiplicity and rational-denominator controls belong to API tests, not a scientific search.

Demo text default and --json strict output, stdout only with no files/stderr from empty directory; Python3.8 grammar compatible. All main report booleans are derived from exact operations, not asserted constants except explicit scope/certification flags. Source and tests remain independent: implementation reconstructs matrix using fixed representation data, tests use independent traces/dimension/index sums and independent form substitution. Existing computational code remains read-only.

## Freeze and bounded verification

Use exact rational arithmetic, not floating ranks or numerical nullspace thresholds. Freeze coefficient bounds, accepted scalar types, matrix conventions, full controls and report schemas after independent derivation/adversarial review and before implementation. Distinguish formal rational directions from physical nonnegative-integer multiplicities. A kernel vector may represent information loss without representing a new allowed physical field.

Deliver one new module/demo/test/derivation quartet with README/ledger links. Prior computational modules and tests remain unchanged. Independently authored tests reconstruct anomaly rows and form-degree substitution rather than round-tripping the new implementation. Preserve any failures/repairs. Run focused tests and23 inherited scientific suites plus all four campaign suites with warnings as errors, eight algebra groups, Python3.8 grammar checks on actual runtime, isolated stdout-only text/strictJSON demos, and before/after source/test hashes. Commit/push/open an ordinary dependent PR after verification. No merge bypass or empirical validation claim.

## Verification record

The sole bounded implementation-author smoke passed without retry. Independent tests were frozen before reading/importing implementation or running controls. Their oracles reconstruct every representation coefficient, use an independent determinant and symbolic RREF/kernel, expand all16 spinor weights with the Ahat genus, and extract the vertical-form coefficient after formal substitution. The first focused run passed167 tests in3.23s with warnings as errors; no failure or repair occurred. Python3.8 grammar passed on actual Python3.9.6. Isolated text and strictJSON demos produced only stdout with no stderr or artifacts, confirming two systems,13 multiplicity controls, three reductions and three parent reports. All eight inherited algebra groups passed before and after implementation.

Frozen files, unchanged across focused and isolated checks:

| File | Lines | SHA256 |
|---|---:|---|
| bpr/chiral_content_constraints.py | 453 | 17f87106ddbc927649c678ec15b52f4dc76c211918bc4cf3e6163ba3932adabc |
| scripts/demo_chiral_content_constraints.py | 96 | e6cdb1e49b0bfa66d82f065b284ea99acc3da49aaea73aca48dcf24e4ae2c74c |
| tests/test_chiral_content_constraints.py | 736 | b78086687a175ef708d5efd618e4f71310cdd1fbbad5c74978c689eb50ba5c22 |

Separate direct code review found no blocker. Executed independent probes passed for all25 charged coefficients, all11 singleton columns across the two systems, exact RREF, permutation-expanded minor determinant, kernel/dependence and Witten parity; formal degree extraction and every domain/kernel vector at flux0,±1,±16; independent16-spinor-weight/Ahat parent reconstruction; and the fixed zero-flux invisible split. Probes also passed strict type/container/coercion rejection, NumPy integer acceptance, canonical coefficient bounds, final invalid coefficient rejection at zero flux, detached/immutable outputs and float-free JSON. Source/demo hashes remained unchanged. The reviewer did not read the independent tests or run their suites. No failed probe or repair occurred.

The combined27-suite regression passed3020 tests in736.24s with warnings as errors and the same frozen hashes. No failed run, implementation/test repair, or prior computational/test modification was required. Exact-arithmetic model verification is not empirical validation.
